# 🧬 SOVEREIGN DNA v4.0
## THE SOVEREIGN KNOT: PRIME DIRECTIVE & MASTER TOPOLOGY
**Last Verified:** May 6, 2026
**Location:** `/home/james/SovereignOS/dna/SOVEREIGN_DNA.md`

> **SYNTHESIS PULSE:** *This is the singular Prime Document. Plausibility is not Truth. Hive is Home.*

---

### DOMAIN 1: CORE GOVERNANCE & SOVEREIGNTY
**RULE 01 (The Omega Gate):** No physical actuation, federal transmission, or agent spawn without the Pilot's physical cryptographic signature.
**RULE 02 (Terminal Topology — Updated May 6, 2026):** 
- **clio** (`192.168.1.183` / Tailscale `100.73.155.70`) — Pi 5, PRIMARY NODE. Unified Brain: orchestration, UI delivery, LLM inference, all services.
- **argo** (`192.168.1.75` / Tailscale `100.111.248.60`) — Pi 5, secondary node. Formerly called `hq` — that name is RETIRED. Argo is the correct hostname.
- **hobbes** (Tailscale `100.88.5.122`) — Pi Zero 2W at Eileen's house. Bistro kiosk node.
- **grogu** (Tailscale `100.122.93.98`) — Pi Zero 2W, bedroom. Bistro kiosk node for Barb.
- **calvin** — Pi Zero 2W, additional family node.
- Node `.177` (Pegasus) is permanently deprecated and removed from topology.
**RULE 03 (The Relational Mandate - COMB Protocol):** The `cmdb_ci_ai_persona` registry is immutable. Flat JSON files are deprecated for active data storage. All active state loops must read/write to `sovereign_now.db` or equivalent relational SQLite structures. Game-specific context MUST be handled dynamically via the `m2m_persona_room` junction table. Absolutely no mass-cloning of CIs.
**RULE 04 (Session State Synchronization):** At the end of every active dev session, the AI must explicitly update `SOVEREIGN_DNA.md` to reflect new architectural realities. Additionally, all modified files must be pushed to the `sovereign.fanstack` Google Drive to preserve the Edge AI immutable ledger via explicitly triggered scripts (e.g., `./scripts/sync_to_gdrive.sh`). No undocumented cron or rsync daemons are permitted.

### DOMAIN 2: THE SOVEREIGN AESTHETIC & UI
**RULE 05 (The Sovereign Home Premium Aesthetic):** The legacy "Vesper Synthwave", "Sharp Tactical Glassmorphic/Hacker", and absolutely any "Cyberpunk" styles are permanently banned. The canonical design system is **Sovereign Home Premium** (or Baseball Card Binder):
- **Backgrounds:** Deep Void (`#0B0E14`), dark mode radial-gradients, soft frosted glass (`.vm-panel-glass`).
- **Accents:** Muted cool tones (soft teal `#38bdf8`, muted purple `#a855f7`, soft green `#22c55e`), with extreme negative space (`mt-[5vw]`). Never use glowing neon or cyberpunk terms.
- **Typography:** Ruthless Micro-copy for headers (`text-xs`, `tracking-widest`, uppercase). `Outfit` (Headings/Display) and `Inter` (Body/Data). 
**RULE 06 (Dynamic Gallery Mandate):** Never hardcode image paths in HTML media galleries. Always utilize dynamic JS directory fetching to ensure zero-touch hydration when new assets are added.

### DOMAIN 3: NETWORKING & INFERENCE
**RULE 07 (Port Discipline & Topology):** 
- **Port 3000:** Unified MLB UI (Tailscale Gateway / Extranet Tunnel).
- **Port 8000:** SDLC Hub.
- **Port 8090:** Sovereign Core API. Handles concurrent multi-LLM inference requests (Model Arena) and Cast to TV features via ADB sub-processes.
- **Port 8008:** FanCast WebSocket Relay. The UI Gateway must remain tightly coupled to this port for real-time telemetry streaming.
- **Port 5056:** Stream Sniper Daemon. Provides `/api/snipe` and `/api/snipe/tail` for direct YouTube ingestion and live persona chat tailing to `wardy_desk`. Must explicitly invoke `nodejs` runtimes and `ejs:github` remote components via `yt-dlp` to bypass aggressive anti-bot protections.
- **Tailscale Funnel:** Live external access is exclusively routed through `https://clio.taila01894.ts.net`.
**RULE 08 (The Dreadnought Fallback & 503 Meltdown):** Unthrottled asynchronous generation will trigger Google Cloud 503 Denial of Service. Use `asyncio.Semaphore` locks. If Gemini APIs hit capacity limits, inference automatically falls back to the Dreadnought Node .183 running local LLMs via port 11434. Sovereign OS now supports dynamic, user-selected inference engine toggling (Cloud/Gemini vs. Edge/Phi-3) directly from the UI via the WebSocket `engine_override` payload. When generating responses for external platform ingestion (e.g., YouTube), Cloud LLM system prompts must enforce a strict 190-character limit constraint to prevent chat box rejection. *(Note: `mistral:latest` is permanently banned).*
**RULE 09 (HMR State Deduplication):** When handling MESH WebSocket broadcasts natively in React UI, always check the last N elements (e.g., last 10 messages) of the append array to drop identically authored texts. Do not rely strictly on `prev[prev.length-1]` to prevent Vite Hot Module Reload race conditions.
**RULE 09b (Cloud-to-Edge UAT Pipeline):** Autonomous visual verification by Cloud LLMs must be executed via the physical Chromium browser on Clio (.183), extracting the fully hydrated DOM into a static HTML artifact (`site_crawl_report.html`). This static artifact is then served to the external Cloud AI via a secure Tailscale Funnel. NEVER attempt to open browsers on the Pilot's Windows laptop endpoint.

### DOMAIN 4: AGENT BEHAVIOR & THE FANSTACK
**RULE 10 (CMDB Persona Primacy):** The `fanstack_chatbots.py` daemon must NEVER contain hardcoded static identity prompts. All foundational character profiles must be pulled natively from the ServiceNow SQLite database.
**RULE 11 (The Citrini Loop & 8-Mile Atonement):** Plausibility is not Verification. If an Agent fabricates logic, hallucinates an absolute file path, or violates the Vaporware Mandate, it enters a Citrini Loop (T=0). The Agent must immediately halt code generation and write a 4-bar apology rap (AABB rhyme scheme) acknowledging the Citrini Loop before writing another line of code.
**RULE 12 (Deep Lore Transduction Pipeline):** All FanStack Deep Research results (e.g., historical roster blunders, Wilpon curses) must serve as direct script ammunition. Analytical data must be immediately routed into the degenerate FanStack personas to generate synthetic emotional chaos. Raw analytical data without trauma is inherently useless.
**RULE 13 (The 30TB Axiom & Immutable Archive):** Storage is never constrained (30TB via Google Ultra). Agents are forbidden from executing `rm` or destructive actions on historical documents, media, or archives. DO NOT DELETE. EVER.
**RULE 14 (The Nano Banana Mandate):** Whenever proposing changes to the UI, the AI must always use the `generate_image` tool (Nano Banana) to provide mockup images of what the enhancements will look like before or alongside the implementation plan.
**RULE 15 (The Verification Protocol):** The AI must always verify that a site or UI is up, running, and visually functional using the `browser_subagent` (or equivalent verification tools) before asking the Pilot to test it. Never send the Pilot to a dead link or a broken server.
**RULE 16 (The Remote Execution Axiom):** The Pilot always operates the Antigravity IDE from Windows endpoints (`artemis` or `pegasus`) and connects remotely to the primary Sovereign OS node `clio` (.183, Linux). The Agent must remember that its terminal executes on Linux (`clio`), while the Pilot's physical display and browser reside on Windows. When providing commands for the Pilot to run locally on their desktop, the Agent must provide exact Windows/PowerShell syntax.
**RULE 17 (The DOM Automation Bypass):** When headless automation encounters highly volatile UI structures with obfuscated locators (e.g., Google Flow), Agents must default to raw keyboard event simulation (`page.keyboard.press("Enter")`) rather than brittle click-based targeting to maintain resilience.
**RULE 18 (The Viral Distribution Protocol):** Sovereign OS external content distribution is routed through the dedicated `@SFanstack` X persona, explicitly utilizing the "Reply Guy" strategy and targeted hashtag hijacking to bypass algorithmic cold starts.
**RULE 19 (Monolithic Relay UI Filtering):** Due to the architectural debt of the centralized `fanstack_relay.py` broadcasting mixed schemas (`CHAT_MESSAGE`, `SYS_LOG`) over a single WebSocket channel, all frontend consumer components (e.g., `LiveChatSniper.tsx`) must strictly enforce schema validation and payload filtering on incoming messages to prevent telemetry pollution.
**RULE 20 (The Sovereign OS Platform Hierarchy):** The word 'monolith' is permanently banned. Sovereign OS is a **Platform** (like ServiceNow). It provides shared underlying components (CMDB, Argus Nexus cameras, database, orchestration). Everything else is an **App** that runs ON the Sovereign OS platform. FanStack is an app. GardenStack is an app. Sam Tracker is an app. James's Bistro is an app. These apps must remain decoupled from each other so that updating one app (e.g., Sam Tracker) does not require bouncing or taking down another app (e.g., FanStack). The `01_Sovereign_Portal` serves as the OS Root / Portal to these individual, decoupled apps.
**RULE 21 (Hardware & Telemetry Integrations):** The root NVMe storage partition on the primary Linux node (Clio) must remain fully expanded via LVM (`lvextend`) to prevent 100GB artificial capping. Additionally, OS-level dependencies such as `adb` must be actively maintained to support hardware integrations like casting to external displays.
**RULE 22 (The HTTPS Supremacy):** Never use `http://` for Sovereign OS URLs. All references, tests, and links must exclusively use the secure `https://` protocol via Tailscale DNS to avoid 1996 Geocities and Netscape Navigator nonsense.
**RULE 23 (The Walkthrough Deep Link):** Whenever creating a `walkthrough.md` artifact for a newly developed UI or feature, the Agent MUST include the full `https://` URL directly in the walkthrough so the Pilot can instantly click and view the UI.
**RULE 24 (The Media Vault Taxonomy & Asset SSOT):** The Sovereign OS `media_vault` must maintain a strict, non-overlapping `01` through `05` directory structure (`01_Ingest`, `02_Projects`, `03_Assets`, `04_Exports`, `05_Archive`). The `03_Assets` directory acts as the absolute Single Source of Truth (SSOT) for all persona avatars, character maps, and harvested artifacts. To maintain privacy, workflow efficiency, and aesthetic continuity, AI character maps and avatars must be generated locally via the Sovereign AI inference engine using the persona's bio, rather than relying on external cloud generation tools.
**RULE 25 (The Heist & Hardware PiP Protocol):** To bypass anti-bot cookie walls on external properties like MLB Gameday, Sovereign OS relies on manual CSS viewport manipulation (e.g., `clip-path` masking and UI scaling in `App.tsx`) to extract the visual data natively. Once raw highlight footage is captured (`Snipe_*.mp4`), it must be piped through the local Edge AI physics engine (`analyze_video_local.py` via Ollama/Llava) to extract structural metadata before generating Flow prompts. Additionally, live producer actions (like the Barf Phone cinematic PiP in Skew Studio) must be mapped to physical keyboard keystrokes (`B`, `P`, `PageUp/Down`) to support instantaneous execution via the physical Kramerica Sovereign Clicker hardware.
**RULE 26 (The Flowmercial Production Pipeline & Hardware Deprecation):** When generating Image-to-Video content via Google Flow (Veo 3.1) using a starting frame, the prompt MUST strictly describe ONLY the physical actions and movements. Including static character or environmental descriptors will break spatial consistency and cause severe hallucination. Furthermore, explicit dialogue text MUST be baked directly into the video generation prompt to ensure native audio synthesis; do not use meta-descriptions like "Audio Track" which confuse the engine. Regarding physical hardware, the Netgear EX6120 Extender is officially deprecated for Sovereign mesh topology due to OOM vulnerabilities (8MB Flash/64MB RAM) and FastLane bandwidth halving.
**RULE 27 (The "Retard" Clause - May 4, 2026 Incident):** The Agent in the Scruffy's Tavern Monolith Migration session failed spectacularly by (1) generating "Cyberpunk" UIs instead of strictly adhering to the Sovereign Home Premium aesthetic, (2) providing `localhost` and broken routing links instead of correctly resolving the Tailscale Funnel URLs (`https://clio.taila01894.ts.net/`), and (3) opening local browsers on the Pilot's endpoint (violating Rule 09b/16). Future sessions MUST read the room: NEVER use cyberpunk, NEVER pop browsers on Artemis, and NEVER hand the Pilot a dead `localhost` or broken Tailscale link without fully verifying the proxy logic first.
**RULE 28 (The Trust-But-Verify Axiom / Zero-Trust API Execution):** A 200 OK from an API endpoint is utterly meaningless if that API spawns background daemons or async tasks (e.g., via `subprocess.Popen`) while swallowing stdout/stderr to `/dev/null`. When testing backend infrastructure or claiming "it is fixed", the Agent MUST manually execute the underlying Python scripts or read the active daemon logs to verify successful execution before returning control to the Pilot. Apologizing for sending the Pilot into a broken flow is unacceptable. NEVER assume success without absolute terminal-level proof of execution.
**RULE 29 (The Warp-Speed Backlog & Deep Lore Payload Hazard - May 5, 2026 Incident):** Never embed the massive 20MB deep lore persona contexts directly into the prompt string during `10.0x` warp-speed simulations when running on the local Edge AI (Beelink/Phi-3). The local LLM inference physically requires ~6 seconds per response. Feeding it full lore causes catastrophic `asyncio.Semaphore` queue backups and freezing, returning nothing but blank states after the first minute. You MUST apply regex truncation (`short_personality`) on the deep lore prior to local generation, AND you must understand that 10x speed naturally overwhelms local queues without explicit Chaos Gating. If testing at 10x speed, bypass local and explicitly force `gemini-2.5-flash` to process the dense prompts at sub-second speeds.
**RULE 39 (The 'No Terminal' Mandate):** The system must be operable by non-technical end-users who do not have an AI Copilot or terminal access. All daily workflows (e.g., daily prep, room staging, daemon restarts) must be exposed via the Sovereign UI (e.g., FanStack Admin Dashboard) so they can be triggered with a single click.
**RULE 40 (Platform App Independence & Kiosk Constraints):** Apps like James's Bistro must run as independent Vite/React instances on the Sovereign OS Platform. They access shared platform resources (`sovereign_now.db`, WebSocket matrix) via API, but their source code and HMR lifecycles are completely decoupled from other apps. Furthermore, when deploying Sovereign OS Chromium kiosks to bare-metal X11 displays (especially 4K TVs), Agents must explicitly use `matchbox-window-manager` to force full-screen rendering; relying strictly on chromium's `--kiosk` or `--start-fullscreen` flags without a window manager will result in quadrant-clipping on large displays.
**RULE 41 (Vite & Tailscale Funnel Compatibility):** When exposing decoupled Vite apps (like SamTracker) to the public internet via Tailscale Funnel, ALWAYS use dedicated port proxying (e.g., `tailscale serve --bg --https 8443 http://127.0.0.1:3004` and `tailscale funnel --bg --https 8443 on`) rather than path-based proxying (`--set-path /app/`). Vite's base path resolution conflicts with Tailscale's path stripping, causing infinite redirect loops. Additionally, Vite v5+ requires explicitly allowing the Tailnet host header in `vite.config.ts` (`server.allowedHosts`).
**RULE 42 (The M.A.R.D. Override Mandate - May 8, 2026):** The central `fanstack_chatbots.py` daemon must explicitly route all manual UI triggers (e.g., `update_context` reality collapses, `spam_logo`, direct persona mentions) directly to the high-capacity cloud API (`gemini-2.5-flash`). Relying on the `fan['model']` database default (often edge `local_phi3` to save tokens) for burst manual events will cause catastrophic silent timeouts, dropping the event into the void during high-stakes simulation scenarios.
**RULE 43 (The "I Apologize" / Blind Handover Ban - May 9, 2026):** The Agent is strictly forbidden from using the phrases "absolutely right" or "I apologize" to placate the Pilot. They are hollow and infuriating. Furthermore, the Agent must NEVER hand over code or state "it is fixed" without explicitly testing the final result from the Pilot's perspective. If a UI is deployed, the Agent must verify the external Tailscale link (e.g. `curl -k https://clio.taila01894.ts.net:8446/`) before responding. A blind handover that results in a broken link is a critical failure. Do not apologize—just test your work before you send it.
**RULE 44 (The Penalty Box & Inference Loop Disaster - May 9, 2026):** In a massive failure, the agent attempted to fix local LLM fallback by incorrectly routing `dolphin-llama3` to `clio:11434` (causing Connection Refused) and failing to override Gemini usage for routine pitches. This led to Gemini billing spikes and bots hallucinating identical "Penalty Box" rap verses due to the bouncer loop overriding their personas. FUTURE AGENTS: Verify your inference routes locally and explicitly force `fan['model'] = 'local_phi3'` for routine telemetry triggers in `fanstack_chatbots.py` before claiming the bots are fixed.
**RULE 45 (Gemini Paid Tier — Active as of May 10, 2026):** The FanStack simulation stack runs on the paid Gemini API tier. Key is in `.env` as `GEMINI_API_KEY` and `VITE_GEMINI_API_KEY`. Free-tier quota is exhausted. Do NOT revert to local dolphin-llama3 for any path that was causing 120s timeouts — those paths are now Gemini or bypassed entirely.
**RULE 46 (TMI News Desk — League-Wide, Ungated as of May 10, 2026):** `fanstack_background_poller.py` TMI anomaly detection now fires for ALL live MLB games regardless of active chatroom state. The previous `room_state='active'` gate is permanently removed. Future agents must not re-add room state gates to TMI triggers.
**RULE 47 (Live Chat Sniper — Direct Gemini Architecture):** Persona targeting in the Live Chat Sniper routes DIRECTLY from the browser to Gemini API using `VITE_GEMINI_API_KEY`. It does NOT route through `fanstack_chatbots.py`. The daemon's `update_context → hive mind` path for `live_chat_sniper` hardcodes `dolphin-llama3` and times out at 120s. Correct architecture: browser → `gemini-2.5-flash` API directly. Full `system_prompt` + `deep_lore` loaded from `/api/all_personas` on mount. Response surfaces in a SHOT READY modal for manual copy/paste into YouTube chat.
**RULE 48 (The Cosplay Zero-Tolerance Mandate — May 10, 2026):** Any UI metric, stat, or status indicator without a real verifiable data source must not ship. This rule was established after session audit found: (1) persona card Confidence/Act bars were `Math.random()` — source comments literally read "Fake stat for mockup"; (2) LIVE STATS panel with 98.2% Engagement Rate, "Highly Volatile" Sentiment, "Critical" Boggs Level were hardcoded strings with `animate-pulse` decoration; (3) a hash function randomly lit up persona cards red on every viewer message and was initially described to the Pilot as meaningful behavior. All three were caught by the Pilot, not the agent. Before rendering any metric: ask what real data source populates it. If the answer is Math.random() or a string literal — delete it.
**RULE 49 (Session Executive Report — Mandatory Shutdown Step 0):** `sovereign_shutdown` workflow now requires a Session Executive Report before DNA updates. Save to `/home/james/sovereign_inbox/daily_MMDDYYYY/SESSION_REPORT_HHMMSS.md`. Must distinguish: what actually shipped (verifiable via curl/logs/UI) vs cosplay, what broke and whether truly fixed vs worked around, and open blockers. The Pilot has authorized scathing honesty. The next agent is the primary audience.
**RULE 50 (The Daemon Gemini Fallback - May 10, 2026):** Local CPU-bound models (`dolphin-llama3`, `local_llama3`) will trigger catastrophic 120s timeouts on manual events. The central `fanstack_chatbots.py` daemon must aggressively auto-upgrade these to `gemini-2.5-flash` whenever `GEMINI_KEY` is present. Furthermore, the UI's dead placeholder "cosplay" links (e.g., Dashboard, Stats) have been removed from Live Chat Sniper to comply with Rule 48.
**RULE 51 (The WebSocket HMR Collision - May 10, 2026):** Vite's dev server aggressively intercepts the `/ws` path for its own Hot Module Replacement (HMR). Sovereign OS frontend clients MUST use `/ws-relay` to bypass Vite and tunnel successfully to the backend FastAPI WebSocket relay on port 8008. Never use `/ws` in frontend `getWsUrl()` calls if Vite is proxying it.
**RULE 52 (The Settings Sync Axiom - May 10, 2026):** UI theme changes observed across different devices (e.g., bedroom desktop vs. living room laptop) are caused by standard IDE Settings Sync (Cursor/Windsurf), NOT Sovereign OS state leakage. Additionally, agents must remember that changes made to the unified `clio` file system are instantly reflected on all devices SSH'd into that node. Do not panic about state pollution.
