# Walkthrough: STRY-06032026-ADVOEXPAND - Hardening Advocate Ingestion Pipeline

This walkthrough details the successful finalization of the modular, PNG-native advocate ingestion pipeline.

## 🚀 Accomplishments

### 1. Ingestion Utility Implementation
- Developed a robust utility [append_brand_advocate.py](file:///home/james/SovereignOS/scripts/append_brand_advocate.py) to dynamically register new advocates.
- The script connects to the canonical SQLite database at `/home/james/SovereignOS/dna/sovereign_now.db`.
- It integrates the `google-genai` Vertex AI SDK to generate high-fidelity, style-referenced avatars using user-provided anchor image sheets as explicit style anchors.
- It includes a 100% offline, local PIL-based fallback engine that crops individual poses from a multi-pose reference grid if Vertex AI is unavailable or fails, ensuring zero initials-based or SVG-based placeholders are generated.

### 2. Seeding of Keith Fanboy and Triple-A Truther Cohorts
- Successfully executed the ingestion pipeline for `@keith_fanboy` using `Keith_Hernandez_fanboy_character_202606031416.jpeg` as the style reference.
- Successfully executed the ingestion pipeline for `@triplea_truther` using `triple_a_truther_Scout_with_clipboard_90s_cartoon_202606031428.jpeg` as the style reference.
- Generated the three canonical PNG poses (`avatar.png`, `pointing.png`, and `shrug.png`) under both `/01_Sovereign_Portal/public/avatars/` and `/15_FanStack/public/avatars/`.
- Verified database mappings in the `persona`, `cmdb_ci_ai_persona`, `cmdb_ci`, and `sys_user` tables.

### 3. Dynamic Bouncer WebSocket Routing
- Updated `scripts/mean_gene.py` to intercept and route comments containing brand advocate keywords: `keith`, `hernandez`, `mustache`, `triplea`, `triple-a`, `maverick`, and `truther`.
- If an unaddressed name drop is made by the Investor, Mean Gene will dynamically display a stylized instruction warning.

---

## 🧪 Verification & Empirical Proof

### A. Database Verification
- Ran SQL queries checking that the advocates are correctly mapped to their canonical PNG paths:
```sql
SELECT id, user_name, display_name, team, avatar_url FROM persona WHERE user_name IN ('keith_fanboy', 'triplea_truther');
```
Output:
```
261a93a87f514987999ee81cbf49b82a|keith_fanboy|KeithsMustache86|NYM|/avatars/keith_fanboy/avatar.png
0c616732ddd54d158c86cebcdec30ca1|triplea_truther|Minor League Maverick|GLOBAL|/avatars/triplea_truther/avatar.png
```

- Verified zero SVG references for the new advocates:
```sql
SELECT COUNT(*) FROM persona WHERE user_name IN ('keith_fanboy', 'triplea_truther') AND avatar_url LIKE '%.svg';
-- Result: 0
```

### B. Asset Path Validation
- Verified the generated PNG files exist on disk:
```bash
ls -l /home/james/SovereignOS/01_Sovereign_Portal/public/avatars/keith_fanboy/
# Output:
# avatar.png
# pointing.png
# shrug.png
```
```bash
ls -l /home/james/SovereignOS/01_Sovereign_Portal/public/avatars/triplea_truther/
# Output:
# avatar.png
# pointing.png
# shrug.png
```

### C. Mean Gene Bouncer Local Verification
- Ran `mean_gene.py` local tests and confirmed proper interception and routing logic:
```
Testing investor routing check:
LFGM! -  {'allow': True, 'action': 'ambient_broadcast'}
Hey 7 train -  {'allow': False, 'action': 'mean_gene_instructional', 'text': ...}
```
