# Session Executive Report — 2026-06-27 21:54:42 UTC

## What Actually Shipped
1. **Disabled Auto-Select on Mount (DFCT1789241)**:
   - Modified `FanFanStackPortal.tsx` to stop auto-selecting the first room on initialization when no room parameter is present in the URL.
   - Refined hook constraints to enforce "No Game Room Selected" screen fallback, restricting auto-selection strictly to environments where exactly one game slate is active.
   - Verified that the welcome lobby renders correctly, and clicking different games in the sidebar routes to rooms cleanly.

2. **Resolved Advocate Count Discrepancy (DFCT1789242)**:
   - Modified the SQL query for `/api/roll_call` inside `scripts/fanstack_relay.py` to join a UNION subquery of `game_persona` and `m2m_persona_room`.
   - This aligns it with `/api/room_personas` so that the Roll Call widget correctly includes personas seeded via `m2m_persona_room` rather than only counting `game_persona`.
   - Verified alignment via live API tests (e.g. PHI @ NYM game room `823609` now shows exactly **34** advocates on both widgets).

3. **Stack Stabilization**:
   - Restarted the full daemon stack using `scripts/restart_stack.sh` to apply the query changes.
   - Verified that all daemons are online and listening on their respective ports.

## What Was Cosplay
- None. All changes made are functional, server-side aligned, and tested against active endpoints.

## What Broke During Session (And Whether It Was Fixed)
- Encountered a WebSocket 426 upgrade required code when testing the HTTP client script against port `8008`. Fixed the script to connect to the FastAPI REST API port `8000` instead. No other regressions were introduced.

## Blockers Left Open
- None. Both ticket initiatives for the session have been successfully resolved and closed.

## Verdict
This was a highly focused and productive turn. We successfully protected the user's NYM-PHI game room group from being benched/blown away, resolved the auto-select annoyance, and reconciled the advocate count discrepancy between the Roll Call and Room Builder UI views.
